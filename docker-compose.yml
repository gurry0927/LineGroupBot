name: linebot

services:
  linebot:
    build: .
    container_name: linebot
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - /data/linebot:/app/data
    environment:
      - TZ=Asia/Taipei
